<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="mybatis.mapper.free">
  
  	<select id="selectFreeList" resultType="domain.Free">
		SELECT FREENO, TITLE, WRITER, HIT
			FROM FREE
		ORDER BY FREENO
	</select>
	
	<select id="selectFreeByNo" parameterType="Long" resultType="domain.Free">
		SELECT FREENO, TITLE, WRITER, CONTENT, IP, HIT
			FROM FREE
		WHERE FREENO = #{freeno}
	</select>
	
	<select id = "getFreeRank" resultType="Long">
		SELECT F.RANK, F.WRITER, F.TITLE, F.IP, F.HIT, F.CONTENT
			FROM (SELECT RANK() OVER (ORDER BY HIT DESC) AS RANK, WRITER, TITLE, IP, HIT, CONTENT FROM FREE) F
		WHERE F.RANK = 1		

	</select>
	
	<insert id="insertFree" parameterType="domain.Free">
		INSERT INTO FREE(FREENO, WRITER, TITLE, CONTENT, IP, HIT)
			VALUES (FREE_SEQ.NEXTVAL, #{writer}, #{title}, #{content}, '127.0.0.1', 0)
	</insert>
	
	<delete id="deleteFree" parameterType="Long">
		DELETE FROM FREE WHERE FREENO = #{freeno}
	</delete>
	
	<update id="updateFree" parameterType="domain.Free">
		UPDATE FREE SET TITLE = #{title}, CONTENT = #{content} WHERE FREENO = #{freeno}
	</update>

  </mapper>